node_modules/
# Every real env file stays local - only .env.example is tracked.
.env
.env.*
!.env.example
*.log
.DS_Store

# generated extension bundles (built by scripts/build-extensions.sh and in Docker)
apps/web/public/downloads/*.zip
.local/

# Vite build output (built in CI/Docker, never committed)
apps/web/dist/

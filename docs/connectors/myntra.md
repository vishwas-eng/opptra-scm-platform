# Myntra Partner / Seller

**Status:** RE scaffold · **UI: Coming soon** · Agent tools blocked.

## Auth
Session/RE primary against Partner portal. Official partner API if/when granted (swap later).

## Actions
`health.ping`, `orders.search`, `asn.status` — all `awaitingHar: true` until sanitized HAR lands.

## Opptra context
`automation-asn` already writes **Myntra ASN XLSX** (with formulas) from UC. Portal ASN acceptance status still needs HAR.

## HAR needed
PO/orders list, inventory, ASN acceptance, returns.
